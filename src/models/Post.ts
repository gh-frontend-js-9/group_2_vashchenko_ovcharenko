export interface Post {
    description: string,
    featuredImage:string,
    category: string[],
    tags: string[],
    likesCount: number,
    viewsCount: number,
    _id: string,
    title: string,
    content: string,
    author: string,
    slug: string,
    __v: number
}